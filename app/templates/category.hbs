<h1>{{model.name}}</h1>

{{model.summary}}

{{#each model.listings as |listing|}}
  {{listing-tile listing=listing destroyListing="destroyListing"}}
{{/each}}

{{new-listing saveListing="saveListing" category=model }}
