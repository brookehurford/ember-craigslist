import { timePosted } from 'craigslist/helpers/time-posted';
import { module, test } from 'qunit';

module('Unit | Helper | time posted');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = timePosted([42]);
  assert.ok(result);
});
